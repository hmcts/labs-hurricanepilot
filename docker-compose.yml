version: '2.1'

services:
  hurricanepilot:
    build:
      context: .
      args:
        - http_proxy
        - https_proxy
        - no_proxy
    image: hmctspublic.azurecr.io/labs/hurricanepilot
    ports:
      - $SERVER_PORT:$SERVER_PORT
